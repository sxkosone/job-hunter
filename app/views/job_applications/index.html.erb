<h1>My job applications</h1>
<% @my_applications.each do |appl| %>
    <h2><%= link_to appl.job.position, application_path(appl) %> at <%= appl.job.company %> <span class="status"><%= appl.status %></span></h2>
    <p><%= appl.job.description %></p>
<% end %>
